install nodejs 16
-----------------------------------

Typescript
---------------------
This was developed by Microsoft. Our browser does not support typescript so we have to convert it into JavaScript.
The extension for typescript is .ts. where as the extension for JavaScript is .js.
We have to compile the typescript and after compilation .ts is converted into .js .
AngularJS use typescript as a scripting language. 
data type of Typescript is let. where as the datatype of JavaScript is var.
--------------------
example1.ts
--------------------
console.log("Welcome to typescript");
console.log("At Mphasis");
----------------------------
step 1:- npm install typescript
             npm install -g typescript
--------------------------------------------------------------------------------------
step 2:-
to compile :-npx tsc example1.ts
after compilation example1.js will be automatically created.
--------------------------------------------------------------------------------------
step 3:-
to run :- node example1.js
--------------------------------------------------------------------
example2.ts
--------------------
function f1()
{
let x=10;
if(x==10)
{
var y=20;
}
console.log(`x=${x}\n y=${y}`);
}

f1();
-------------------------------------------------------------
After compilation the .js created is
---------------------------------------------------
function f1() {
    var x = 10;
    if (x == 10) {
        var y = 20;
    }
    console.log("x=".concat(x, "\n y=").concat(y));
}
f1();
-------------------------------------------------------------------------------------
example3.ts
------------------------
let username:string="Rakshita";
let age:number=20;
let address:string="Bangalore";
console.log("Hello !"+"  "+username+"you be be "+(age+1) +"next year");
console.log(`Your address is ${address}`);
------------------------------------------------------------------------------------------------
step 1:- npm install -g typescript@latest
step 2:-notepad example1.ts
step 3:-compile ---->  tsc example1.ts
step 4:-to run ---->      node example1.js
-----------------------------------------------------------------------------
example-4
-----------------
var user="Sandip";
var firstChar=user.charCodeAt(0);
if(firstChar >=65 && firstChar <=90)
{
console.log("Hello ! "+user);
}
else
{
console.error("Error :Name must start with Uppercase Letter");
}
-------------------------------------------------------------------------------------------------
example-5
-------------------
var x="These is typescript example";
if(x.startsWith("This"))
{
console.log("The example is of Typescript");
}
else if(x.startsWith("These"))
{
console.log("The example is of Javascript");
}
else
{
console.log("The example is of  NodeJs");
}
------------------------------------------------------------------------------------
example-6
-------------------
var x="This is typescript";
if(x.endsWith("typescript"))
{
console.log("The example is of Typescript");
}
else if(x.endsWith("javascript"))
{
console.log("The example is of Javascript");
}
else
{
console.log("The example is of  NodeJs");
}
------------------------------------------------------------------------------
example-7
-----------------------
var x="swarna@gmail.com";
if(x.indexOf("@")==-1)
{
console.log("The email is invalid");
}
else
{
console.log("The email is valid");
}
----------------------------------------------------
*-1 indicates not present
----------------------------------------------------------------------------
example-8
----------------------
let x:string="This is swarna's email swarna@gmail.com";
if(x.includes("swarna"))
{
console.log("This is about swarna");
}
else if(x.includes("pratiksha"))
{
console.log("This is about pratiksha");
}
else
{
console.log("This is about other students");
}
-----------------------------------------------------------------------------------------------------------
example-9
------------------
let x:string="SamsungTV,45000.34,InStock";
let data:string[]=x.split(',');
for(var value of data)
{
console.log(value);
}
console.log(`Name = ${data[0]}`);
console.log(`Price = ${data[1]}`);
console.log(`Instock Or Not = ${data[2]}`);
------------------------------------------------------------------------
example-10
------------------------
let password:string="admin";
if(password.trim()==" admin ")
{
console.log("correct password");
}
else
{
console.log("wrong password");
}

--------------------------------------------------------------------------------
example-11
-----------------------
let Name="Samsung TV";
let Price=45000.00;
if(Price==undefined)
{
console.log("Name ="+Name);
}
else
{
console.log("Name ="+Name +"\n Price ="+Price);
}
--------------------------------------------------------------------------------------------
example-12
------------------------
let Name:string|null =prompt("Enter your name");
let Address:string =prompt("Enter your address");

if(Name=="null")
{
console.log("Name is Null");
}
else if(Name=="" && Address=="")
{
console.log("Name and Address is empty");
}
else
{
console.log("Hello !"+Name);
}
-----------------------------------------------------------------------------
<html>
<head>
<script src="example12.js"></script>
</head>
</html>
----------------------------------------------------------------------------------
example-13
------------------------
let x:string[] = ["sandip","akshaya","Totendra","swarna","pratiksha"];
x[1]="Rakshita";
for(var item of x)
{
console.log(item);
}
---------------------------------------------------
example-14
---------------------
let x:number[] = [1,2,3,4,5,6,7];
x[1]=10;
for(var item of x)
{
console.log(item);
}
---------------------------------------------------------------------
example-15
---------------------
let x:any[] = [1,2,3,"sandip","akshaya",[1,2,3,4],4];
for(var item of x)
{
console.log(item);
}
--------------------------------------------------------------------
example-16
----------------------
let x:any[] = [1,2,3,"sandip","akshaya",[1,2,3,4],4];
for(var item of x[3])
{
console.log(item);
}
console.log(x[0]);
console.log(x[1]);
console.log(x[2]);
console.log(x[3]);
console.log(x[4]);
console.log(x[5]);
----------------------------------------------------------------------------
example-17
---------------------
let x:any[] = [1,2,3,"sandip","akshaya",[1,2,3,4],4];
for(var property in x)
{
console.log(`${property} : ${x[property]}`);
}

-------------------------------------------------------------
example-18
----------------------
let x:string[] = ["electronics","fashion","footwear"];

function PrintList()
{
for(var property in x)
{
console.log(`${property} : ${x[property]}`);
}
}

PrintList();
-------------------------------------------------------------------
example-19
-----------------
interface IProduct
{
Name:string;
Address:string;
Price:number;
Instock:boolean;
}

function getdata<T>(obj:T)
{
for(var property in obj)
{
console.log(`${property} : ${obj[property]}`);
}
}

getdata<IProduct>({Name:'Samsung TV',Address:'Bangalore',Price :35000,Instock:true});

getdata<IProduct>({Name:'Samsung Fridge',Address:'Bangalore',Price :45000,Instock:false});

--------------------------------
---------------------------------------------------------------------------------------------------------------------------------------------
interface is a keyword  in typescript is similar to structure in c.
It is a datatype having a mixture of datatypes.
---------------------------------------------------------------------------------------------------------- 
example- 20
---------------------------
interface IProduct
{
Name:string;
Price:number;
Instock:boolean;
}

interface IEmployee
{
Name:string;
Salary:number;
Address:string;
Age:number;
}


function getdata<T>(obj:T)
{
for(var property in obj)
{
console.log(`${property} : ${obj[property]}`);
}
}

getdata<IProduct>({Name:'Samsung TV',Price :35000,Instock:true});

getdata<IProduct>({Name:'Samsung Fridge',Price :45000,Instock:false});

getdata< IEmployee>({Name:'sandip',Salary :45000,Address:"Banglore",Age:35});

getdata< IEmployee>({Name:'Totendra',Salary :45000,Address:"Banglore",Age:20});

-------------------------------------------------------------------------------------------------------------
The TypeScript access modifiers are of three types. 
Public, Private, Protected.

Public
In TypeScript by default, all the members (properties and methods) of a class are public. We can access this data member anywhere without any restriction.

Private
The private access modifier cannot be accessible outside of its containing class. It ensures that the class members are visible only to that class in which it is containing.

Protected
A Protected access modifier can be accessed only within the class and its subclass. We cannot access it from the outside of a class in which it is containing.

-------------------------------------------------------------------------------------------------------------

example-21
---------------------------------
class Student 
{  
    public studCode: number;  
    studName: string;  
}  
  
let stud = new Student();  
stud.studCode = 101;  
stud.studName = "Sandip Kumar";  
  
console.log(stud.studCode+ " "+stud.studName);  
------------------------------------------------------------------------------------------------
*In the above example, studCode is public, and studName is declared without a modifier, so TypeScript treats them as public by default. Since data members are public, they can be accessed outside of the class using an object of the class.
--------------------------------------------------------------------------------------------------------
example-22
---------------------------
class Student 
{  
public studCode: number;  
private studName: string;  
constructor(code: number, name: string)
{  
this.studCode = code;  
this.studName = name;  
}  
public display() {  
return (`My unique code: ${this.studCode}, my name: ${this.studName}.`);  
}  
}  
  
let student: Student = new Student(1, "Sandip");  
console.log(student.display());  
-----------------------------------------------------------------------------------------------
*studCode is private, and studName is declared without a modifier, so TypeScript treats it as public by default. If we access the private member outside of the class, it will give a compile error.
------------------------------------------------------------------------------------------------------
example-23
----------------------------
class Student {  
    public studCode: number;  
    protected studName: string;  
    constructor(code: number, name: string){  
        this.studCode = code;  
        this.studName = name;  
        }  
}  
class Person extends Student {  
    private department: string;  
  
    constructor(code: number, name: string, department: string) {  
        super(code, name);  
        this.department = department;  
    }  
    public getDetails() {  
        return (`My unique code: ${this.studCode}, my name: ${this.studName} and I am in ${this.department} Branch.`);  
    }  
}  
let obj: Person = new Person(1, "sandip", "CS");  
console.log(obj.getDetails());  
---------------------------------------------------------------------------------------------------------------
*A Protected access modifier can be accessed only within the class and its subclass. We cannot access it from the outside of a class in which it is containing.
---------------------------------------------------------------------------------------------------------------
Interface Inheritance

We can inherit the interface from the other interfaces. In other words, Typescript allows an interface to be inherited from zero or more base types.
The base type can be a class or interface. We can use the "extends" keyword to implement inheritance among interfaces.
--------------------------------------
example-24
--------------------------------------
interface Person 
{   
name:string    
}  

interface PersonDetail 
{   
    age:number  
    gender:string  
}
  
interface Employee extends Person, PersonDetail 
{   
    empCode:number  
}  

let empObject = <Employee>{};   
empObject.name = "Abhishek"  
empObject.age = 25   
empObject.gender = "Male"  
empObject.empCode = 43  

console.log("Name: "+empObject.name);  
console.log("Employee Code: "+empObject.empCode);  
console.log("Age: "+empObject.age);  
console.log("Employee Gender: "+empObject.gender);  
--------------------------------------------------------------------------------------------------------
example-25
----------------------
// Array which return string  
interface nameArray {  
    [index:number]:string  
}  
// use of the interface  
let myNames: nameArray;  
myNames = ['Virat', 'Rohit', 'Sachin'];  
console.log("My name is: " +myNames[1]);  
  
// Array which return number  
interface ageArray {  
    [index:number]:number  
}  
var myAges: ageArray;  
myAges =[10, 18, 25];  
console.log("My age is: " +myAges[1]);  
--------------------------------------------------------------------------------------------------------------
*In the above example, we have declared nameArray that returns string and ageArray that returns number. 
The type of index in the array is always number so that we can retrieve array elements with the use of its index position in the array.
---------------------------------------------------------------------------------------------------------------
example-26
-------------------------------------------
// defining interface for class  
interface Person {  
    firstName: string;  
    lastName: string;  
 age: number;  
    FullName();  
    GetAge();  
}  
// implementing the interface  
class Employee implements Person {  
    firstName: string;  
    lastName: string;  
    age:number;  
    FullName() {  
        return this.firstName + ' ' + this.lastName;  
    }  
    GetAge() {  
        return this.age;   
}  
    
    constructor(firstN: string, lastN: string, getAge: number) {  
        this.firstName = firstN;  
        this.lastName = lastN;  
        this.age = getAge;  
    }  
}  
// using the class that implements interface  
let myEmployee = new Employee('Abhishek', 'Mishra', 25);  
let fullName = myEmployee.FullName();  
let Age = myEmployee.GetAge();  
console.log("Name of Person: " +fullName + '\nAge: ' + Age);  
----------------------------------------------------------------------------------------------------------------------------
*In the above example, we have declared Person interface with firstName, lastName as property and FullName and GetAge as method/function. The Employee class implements this interface by using the implements keyword. After implementing an interface, we must declare the properties and methods in the class. If we do not implement those properties and methods, it throws a compile-time error. We have also declared a constructor in the class. So when we instantiate the class, we need to pass the necessary parameters otherwise it throws an error at compile time.
---------------------------------------------------------------------------------------------------------------
